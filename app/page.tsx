/* eslint-disable @typescript-eslint/no-explicit-any, @typescript-eslint/no-unused-vars, react-hooks/set-state-in-effect, react-hooks/exhaustive-deps */
"use client";
import {FormEvent,useEffect,useMemo,useRef,useState} from "react";
import {usePathname} from "next/navigation";
import TodayRecommendations from "./components/TodayRecommendations";
import AiPredictionReport from "./components/AiPredictionReport";
import ArchiveNavLink from "./components/ArchiveNavLink";
import type {PredictionCoverageSummary,UnavailablePredictionMatch} from "./components/PredictionCoverage";
import PredictionMarketSignals from "./components/PredictionMarketSignals";
import MarketPredictionTable from "./components/MarketPredictionTable";
import {SavedPredictionSet} from "./prediction-config";
import {readBrowserData,writeBrowserData,savePredictionSet,writeLocalData,reportStorageWarning} from "./browser-storage";
import {fetchOfficialSporttery} from "./sporttery-official";
import {buildOfficialPredictionFallback} from "./official-prediction-fallback";
type Pick={matchId:string;market:string;label:string;odd:number};
type Rec={id:number;date?:string;match:string;type?:string;pick:string;stake:number;odd:number;result:"待定"|"命中"|"未中";note:string};
const markets={
 "胜平负":["胜","平","负"],
 "让球胜平负":["让胜","让平","让负"],
 "比分":["1:0","2:0","2:1","3:0","3:1","3:2","4:0","4:1","4:2","5:0","5:1","5:2","胜其他","0:0","1:1","2:2","3:3","平其他","0:1","0:2","1:2","0:3","1:3","2:3","0:4","1:4","2:4","0:5","1:5","2:5","负其他"],
 "总进球数":["0","1","2","3","4","5","6","7+"],
 "半全场":["胜胜","胜平","胜负","平胜","平平","平负","负胜","负平","负负"]
} as const;
type Market=keyof typeof markets;
type MarketState="available"|"unavailable"|"failed";
type DataState="loading"|"success"|"stale"|"empty"|"error";
type PoolStatus=Record<string,{status:"success"|"failed";matchCount:number;error?:string}>;
type MarketEligibility={marketCode:string;handicap:string|null;salesStatus:string;supportsSingle:boolean;allowedPassCounts:number[];cutoffAt:string|null;ruleVersion:string;qualification:string};
type Match={id:string;league:string;time:string;home:string;away:string;odds?:number[]|null;form:string[];tag:string;risk:string;matchId?:string;officialMatchId?:string;salesDate?:string;kickoffAt?:string;homeTeamId?:string;awayTeamId?:string;homeTeamCode?:string;awayTeamCode?:string;matchDate?:string;homeRank?:string;awayRank?:string;sellStatus?:number;matchStatus?:string;remark?:string;handicap?:string|null;updatedAt?:string;marketOdds?:Partial<Record<Market,number[]|null>>;marketStatus?:Partial<Record<Market,MarketState>>;marketEligibility?:Partial<Record<Market,MarketEligibility>>};
type OddsPoint={updateDate?:string;updateTime?:string;h?:string;d?:string;a?:string;goalLine?:string;hf?:string;df?:string;af?:string};
type DetailData={sourceUrl:string;match?:{matchDateTime?:string;homeTeamShortName?:string;awayTeamShortName?:string};oddsHistory?:Array<{hadList?:OddsPoint[];hhadList?:OddsPoint[];ttgList?:Record<string,string>[];hafuList?:Record<string,string>[];crsList?:Record<string,string>[]}>};
type MatchResult={id:string;matchId:string;date:string;league:string;home:string;away:string;halfScore:string;fullScore:string;handicap:string;status:string};
const handicapText=(value:unknown)=>{const text=String(value??"").trim();if(!text)return"—";const numeric=Number(text);return Number.isFinite(numeric)?`${numeric>0?"+":""}${numeric}`:text};
type CompanyPredictionOdds={companyId:number;company:string;win:number;draw:number;lose:number;handicap:number;homePrice:number;awayPrice:number;total:number;overPrice:number;underPrice:number;firstWin:number;firstDraw:number;firstLose:number;firstHandicap:number;firstHomePrice:number;firstAwayPrice:number;firstTotal:number;firstOverPrice:number;firstUnderPrice:number};
type ScorePrediction={score:string;probability:number};
type PredictionVersion={predictionId:string;inputSnapshotId:string;baseModelVersion:string;calibrationVersion:string;aiReviewVersion?:string|null;reviewForPredictionId?:string;parentPredictionId?:string|null;generatedAt:string;aiReviewedAt?:string};
type PredictionReport={predictionId?:string;inputSnapshotId?:string;baseModelVersion?:string;calibrationVersion?:string;predictionGeneratedAt?:string;fullScoreDistribution?:ScorePrediction[];shadowFullScoreDistribution?:ScorePrediction[];shadowHadProbabilities?:ScorePrediction[];appliedIntelligenceWeight?:number;intelligenceEvidence?:{records:Array<{type:string;sourceUrl:string;observedAt:string;summary?:string}>};id:string;officialMatchId?:string;salesDate?:string;kickoffAt?:string;homeTeamId?:string;awayTeamId?:string;homeTeamCode?:string;awayTeamCode?:string;officialMappingStatus?:string;mappingReason?:string;marketEligibility?:Partial<Record<Market,MarketEligibility>>;league:string;time:string;matchDate?:string;home:string;away:string;matchStatus?:string;isMock?:boolean;sourceUpdatedAt?:string;companies:CompanyPredictionOdds[];marketProbabilities?:number[];probabilities:{home:number;draw:number;away:number};consensus:{handicap:number;totalLine:number;agreement:string};marketSignal?:{direction:string;strength:number;probabilityShifts:number[];fairOdds:number[];hadEv?:number[];hhadEv?:number[];evThreshold?:number;institutionAction?:string;handicapExpectation?:string;firstHandicap:number;handicapChange:number;narrative:string;officialOdds:number[];officialHandicap:string;officialHhadOdds:number[];officialHhadFair:number[];modeledHhad:number[];hhadAvailable?:boolean;modeledTotalGoals?:number[];modeledHalfFull?:number[];asianHomeProbability:number;asianAwayProbability:number;asianMovement:number;overProbability:number;fitAgreement:string;handicapMeaning:string};expectedGoals:{home:number;away:number};scores:ScorePrediction[];oddsScores?:ScorePrediction[];intelligenceScores?:ScorePrediction[];intelligenceCoverage?:number;hadProbabilities?:ScorePrediction[];hhadProbabilities?:ScorePrediction[];totalGoalProbabilities?:ScorePrediction[];missingCompanies:number[];aiSummary?:string;aiRisk?:string};
const shanghaiDate=(offsetDays=0)=>new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Shanghai",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date(Date.now()+offsetDays*86400000));
// 所有赛事筛选与场次排序按竞彩开售日（周五001 等）而非实际开赛日。
// 因此周五深夜开赛、周六凌晨结束的周五009，仍属于“周五场次”。
const matchDateKey=(match:{salesDate?:string;matchDate?:string;time:string})=>{
 const raw=String(match.salesDate||match.matchDate||match.time||"");
 const parts=raw.match(/(\d{4})[-/](\d{1,2})[-/](\d{1,2})/);
 return parts?`${parts[1]}-${parts[2].padStart(2,"0")}-${parts[3].padStart(2,"0")}`:"";
};
const compareMatchesByDateAndSequence=(left:Match,right:Match)=>{
 const dateOrder=matchDateKey(left).localeCompare(matchDateKey(right));if(dateOrder)return dateOrder;
 const sequenceOrder=left.id.localeCompare(right.id,"zh-CN",{numeric:true,sensitivity:"base"});if(sequenceOrder)return sequenceOrder;
 return String(left.kickoffAt||left.time).localeCompare(String(right.kickoffAt||right.time));
};
const matchDateLabel=(date:string)=>{
 const formatted=new Intl.DateTimeFormat("zh-CN",{timeZone:"Asia/Shanghai",month:"numeric",day:"numeric",weekday:"short"}).format(new Date(`${date}T12:00:00+08:00`));
 return `${formatted.replace("星期","周")}场次`;
};
const recordDate=(record:Rec)=>record.date||(record.id>1_000_000_000_000?new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Shanghai",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date(record.id)):"");
const SPORTTERY_CACHE_KEY="ff-sporttery-official-cache-v1";
const matchCacheKey=(match:Match)=>`${match.salesDate||match.matchDate||""}:${match.officialMatchId||match.matchId||match.id}`;
const mergeOfficialMatches=(current:Match[],incoming:Match[])=>{
 const merged=new Map(current.map(match=>[matchCacheKey(match),match]));
 incoming.forEach(match=>{const key=matchCacheKey(match),previous=merged.get(key);merged.set(key,previous?{...previous,...match,marketOdds:{...previous.marketOdds,...Object.fromEntries(Object.entries(match.marketOdds||{}).filter(([,value])=>Array.isArray(value)&&value.some(odd=>Number(odd)>0)))},marketStatus:{...previous.marketStatus,...match.marketStatus},marketEligibility:{...previous.marketEligibility,...match.marketEligibility}}:match)});
 return Array.from(merged.values());
};
const oddsFor=(game:Match|undefined,market:Market):number[]|null=>{const candidate=game?.marketOdds?.[market]??(market==="胜平负"?game?.odds:null);if(!Array.isArray(candidate)||!candidate.some(value=>Number.isFinite(Number(value))&&Number(value)>0))return null;return candidate.map(value=>Number.isFinite(Number(value))&&Number(value)>0?Number(value):0)};
const fairMarketPoints=(labels:string[],odds:number[])=>{const raw=odds.map(value=>value>1?1/value:0),total=raw.reduce((sum,value)=>sum+value,0);return total>0?labels.map((score,index)=>({score,probability:raw[index]/total*100})):[]};
export default function Home(){
 const pathname=usePathname();
 const view=pathname==="/recommendations"?"recommendations":pathname==="/market-predictions"?"market-predictions":pathname==="/predictions"?"predictions":pathname==="/prediction-archive"?"prediction-archive":"matches";
 const[picks,setPicks]=useState<Pick[]>([]),[stake,setStake]=useState(20),[budget]=useState(200),[records,setRecords]=useState<Rec[]>([]),[ready,setReady]=useState(false),[betMatch,setBetMatch]=useState(""),[betMarket,setBetMarket]=useState<Market>("胜平负"),[detailMatch,setDetailMatch]=useState<string|null>(null),[detailMarket,setDetailMarket]=useState<Market>("胜平负"),[aiLoading,setAiLoading]=useState(false),[aiResult,setAiResult]=useState(""),[aiError,setAiError]=useState(""),[liveMatches,setLiveMatches]=useState<Match[]>([]),[dataLoading,setDataLoading]=useState(true),[dataState,setDataState]=useState<DataState>("loading"),[dataError,setDataError]=useState(""),[dataMeta,setDataMeta]=useState<{upstreamUpdatedAt:string;fetchedAt:string;poolStatus?:PoolStatus;deliveryMode?:string}>({upstreamUpdatedAt:"",fetchedAt:""}),[detailData,setDetailData]=useState<DetailData|null>(null),[detailDataLoading,setDetailDataLoading]=useState(false),[detailDataError,setDetailDataError]=useState("");
 const[leagueFilter,setLeagueFilter]=useState("全部比赛");
 const[dateFilter,setDateFilter]=useState("全部场次");
 const[jumpMatch,setJumpMatch]=useState("");
 const[hideClosed,setHideClosed]=useState(false);
 const[resultDate,setResultDate]=useState(()=>shanghaiDate(-1));
 const[resultRows,setResultRows]=useState<MatchResult[]>([]);
 const[resultLoading,setResultLoading]=useState(false);
 const[resultError,setResultError]=useState("");
 const[repairNotice,setRepairNotice]=useState("");
 const[predictionRows,setPredictionRows]=useState<PredictionReport[]>([]);
 const[predictionCoverage,setPredictionCoverage]=useState<PredictionCoverageSummary|null>(null);
 const[unavailablePredictions,setUnavailablePredictions]=useState<UnavailablePredictionMatch[]>([]);
 const[predictionLoading,setPredictionLoading]=useState(false);
 const[predictionError,setPredictionError]=useState("");
 const[predictionMeta,setPredictionMeta]=useState({fetchedAt:"",sourceUrl:"",methodology:""});
 const[predictionVersion,setPredictionVersion]=useState<PredictionVersion|null>(null);
 const[predictionAiLoading,setPredictionAiLoading]=useState(false);
 const[predictionAiError,setPredictionAiError]=useState("");
 const[predictionAiProvider,setPredictionAiProvider]=useState("");
 const[predictionRetryNonce,setPredictionRetryNonce]=useState(0);
 const predictionRetryHandledRef=useRef(0);
 const allMatches=useMemo(()=>liveMatches.slice().sort(compareMatchesByDateAndSequence),[liveMatches]);
 const predictionSalesDate=shanghaiDate(),predictionMatches=useMemo(()=>allMatches.filter(match=>matchDateKey(match)===predictionSalesDate),[allMatches,predictionSalesDate]);
 const leagues=useMemo(()=>["全部比赛",...Array.from(new Set(allMatches.map(match=>match.league))).sort((a,b)=>a.localeCompare(b,"zh-CN"))],[liveMatches]);
 const matchDates=useMemo(()=>Array.from(new Set(allMatches.map(matchDateKey).filter(Boolean))).sort(),[allMatches]);
 const matches=allMatches.filter(match=>{const status=(match.matchStatus||"").toLowerCase(),closed=status&&status!=="selling";const query=jumpMatch.trim().toLowerCase(),date=matchDateKey(match);return(dateFilter==="全部场次"||date===dateFilter)&&(leagueFilter==="全部比赛"||match.league===leagueFilter)&&(!query||`${match.id} ${match.home} ${match.away}`.toLowerCase().includes(query))&&(!hideClosed||!closed)});
 useEffect(()=>{try{const raw=localStorage.getItem("ff-records");if(raw){const parsed=JSON.parse(raw);if(!Array.isArray(parsed))throw new Error("记录格式无效");setRecords(parsed)}setReady(true)}catch{reportStorageWarning("ff-records","投注记录暂时无法读取，已停止自动覆盖，原有记录未修改。")}},[]);
 useEffect(()=>{if(ready)writeLocalData("ff-records",records)},[records,ready]);
 async function requestSporttery(repair=false){
  try{
   const response=await fetch(`/api/sporttery${repair?"?repairMissing=1":""}`,{cache:"no-store"}),raw=await response.text();
   let data:any={};try{data=raw?JSON.parse(raw):{}}catch{throw new Error(`站点数据接口返回异常（HTTP ${response.status}）`)}
   if(!response.ok)throw new Error(data.error||"官方数据读取失败");
   return data;
  }catch(internalError){
   try{return await fetchOfficialSporttery({repair,serverHeaders:false,timeoutMs:12000})}
   catch(directError){throw new Error(`站点接口：${internalError instanceof Error?internalError.message:"读取失败"}；浏览器直连：${directError instanceof Error?directError.message:"读取失败"}`)}
  }
 }
 async function refreshSporttery(fallback?:{matches:Match[];upstreamUpdatedAt?:string;fetchedAt?:string;poolStatus?:PoolStatus;deliveryMode?:string}){const cached=fallback||{matches:liveMatches,...dataMeta};setDataLoading(true);setDataState("loading");setDataError("");try{const data=await requestSporttery(false);const nextMatches=Array.isArray(data.matches)?data.matches:[];setLiveMatches(nextMatches);setDataMeta({upstreamUpdatedAt:data.upstreamUpdatedAt||"",fetchedAt:data.fetchedAt||"",poolStatus:data.poolStatus,deliveryMode:data.deliveryMode});setDataState(nextMatches.length?"success":"empty");void writeBrowserData(SPORTTERY_CACHE_KEY,{matches:nextMatches,upstreamUpdatedAt:data.upstreamUpdatedAt||"",fetchedAt:data.fetchedAt||"",poolStatus:data.poolStatus,deliveryMode:data.deliveryMode});if(nextMatches.length)setBetMatch((current:string)=>nextMatches.some((m:Match)=>m.id===current)?current:nextMatches[0].id);else{setBetMatch("");setPicks([])}}catch(error){const message=error instanceof Error?error.message:"官方数据读取失败";setDataError(message);if(cached.matches.length){setLiveMatches(cached.matches);setDataMeta({upstreamUpdatedAt:cached.upstreamUpdatedAt||"",fetchedAt:cached.fetchedAt||"",poolStatus:cached.poolStatus,deliveryMode:cached.deliveryMode});setDataState("stale")}else{setLiveMatches([]);setDataState("error")}}finally{setDataLoading(false)}}
 async function repairMissingMatches(){const before=liveMatches;setDataLoading(true);setRepairNotice("");try{const data=await requestSporttery(true);const incoming=Array.isArray(data.matches)?data.matches:[],nextMatches=mergeOfficialMatches(before,incoming),beforeMarkets=before.reduce((sum,match)=>sum+Object.values(match.marketOdds||{}).filter(Boolean).length,0),afterMarkets=nextMatches.reduce((sum,match)=>sum+Object.values(match.marketOdds||{}).filter(Boolean).length,0),added=nextMatches.length-before.length,completed=Math.max(0,afterMarkets-beforeMarkets);setLiveMatches(nextMatches);setDataMeta({upstreamUpdatedAt:data.upstreamUpdatedAt||dataMeta.upstreamUpdatedAt,fetchedAt:data.fetchedAt||new Date().toISOString(),poolStatus:data.poolStatus,deliveryMode:data.deliveryMode});setDataState(nextMatches.length?"success":"empty");setDataError("");setRepairNotice(added||completed?`补抓完成：新增 ${added} 场，补全 ${completed} 个玩法。`:`补抓完成，官方本次未返回新的场次或玩法；已有数据保持不变。`);void writeBrowserData(SPORTTERY_CACHE_KEY,{matches:nextMatches,upstreamUpdatedAt:data.upstreamUpdatedAt||dataMeta.upstreamUpdatedAt,fetchedAt:data.fetchedAt||new Date().toISOString(),poolStatus:data.poolStatus,deliveryMode:data.deliveryMode})}catch(error){setRepairNotice(`补抓失败：${error instanceof Error?error.message:"请稍后重试"}；已有比赛未受影响。`)}finally{setDataLoading(false)}}
 async function retryUnavailablePredictionData(){
  if(dataLoading||predictionLoading||!unavailablePredictions.length)return;
  setRepairNotice("正在重新抓取官方赛事和外围盘口，并重新核验主客队名称…");
  await repairMissingMatches();
  setPredictionRetryNonce(value=>value+1);
 }
 useEffect(()=>{let active=true;void readBrowserData<{matches:Match[];upstreamUpdatedAt?:string;fetchedAt?:string;poolStatus?:PoolStatus;deliveryMode?:string}|null>(SPORTTERY_CACHE_KEY,null).then(parsed=>{if(!active)return;const cached=Array.isArray(parsed?.matches)?parsed:undefined;if(cached?.matches.length){setLiveMatches(cached.matches);setDataMeta({upstreamUpdatedAt:cached.upstreamUpdatedAt||"",fetchedAt:cached.fetchedAt||"",poolStatus:cached.poolStatus,deliveryMode:cached.deliveryMode});setDataState("stale")}void refreshSporttery(cached)});return()=>{active=false}},[]);
 useEffect(()=>{if(view!=="results")return;let active=true;setResultLoading(true);setResultError("");fetch(`/api/sporttery/results?date=${resultDate}`,{cache:"no-store"}).then(async response=>{const data=await response.json();if(!response.ok)throw new Error(data.error||"赛果读取失败");if(active)setResultRows(data.results||[])}).catch(error=>{if(active){setResultRows([]);setResultError(error instanceof Error?error.message:"赛果读取失败")}}).finally(()=>{if(active)setResultLoading(false)});return()=>{active=false}},[view,resultDate]);
 useEffect(()=>{
  if((view!=="predictions"&&view!=="market-predictions")||dataLoading)return;
  setPredictionCoverage(null);setUnavailablePredictions([]);
  if(dataState!=="success"||!predictionMatches.length){setPredictionRows([]);setPredictionVersion(null);setPredictionError(dataState==="stale"?"官方数据已过期，已暂停生成新预测。":dataState==="error"?"官方数据读取失败，无法生成预测。":`${predictionSalesDate} 竞彩销售日暂无可用于预测的官方赛事。`);return}
  let active=true;
  const forceRefresh=predictionRetryNonce>0&&predictionRetryHandledRef.current!==predictionRetryNonce;
  if(forceRefresh)predictionRetryHandledRef.current=predictionRetryNonce;
  setPredictionLoading(true);setPredictionError("");
  fetch("/api/predictions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({forceRefresh,matches:predictionMatches.map(match=>({id:match.id,matchId:match.matchId,officialMatchId:match.officialMatchId||match.matchId,salesDate:match.salesDate,kickoffAt:match.kickoffAt,homeTeamId:match.homeTeamId,awayTeamId:match.awayTeamId,homeTeamCode:match.homeTeamCode,awayTeamCode:match.awayTeamCode,league:match.league,time:match.time,matchDate:match.matchDate,home:match.home,away:match.away,odds:oddsFor(match,"胜平负"),handicap:match.handicap,hhadOdds:oddsFor(match,"让球胜平负"),marketEligibility:match.marketEligibility,matchStatus:match.matchStatus,updatedAt:match.updatedAt,isMock:false}))})}).then(async response=>{
   const data=await response.json();if(!response.ok)throw new Error(data.error||"今日赔率读取失败");if(!active)return;
   const reports=(data.reports||[]).filter((row:PredictionReport)=>row.predictionId===data.predictionId&&row.officialMappingStatus==="verified"&&predictionMatches.some(match=>(match.officialMatchId||match.matchId)===row.officialMatchId&&matchDateKey(match)===matchDateKey(row))).map((row:PredictionReport)=>({...row,oddsScores:row.fullScoreDistribution||row.scores,combinedScores:row.scores,intelligenceScores:undefined,intelligenceCoverage:0,aiSummary:undefined,aiRisk:undefined}));
   const unavailable=predictionMatches.filter(match=>!reports.some((row:PredictionReport)=>row.officialMatchId===(match.officialMatchId||match.matchId)&&matchDateKey(match)===matchDateKey(row))).map(match=>{
    const detail=(Array.isArray(data.unavailableOfficialMatches)?data.unavailableOfficialMatches:[]).find((row:UnavailablePredictionMatch)=>row.officialMatchId===(match.officialMatchId||match.matchId));
    return{id:match.id,officialMatchId:match.officialMatchId||match.matchId,salesDate:match.salesDate,matchDate:match.matchDate,kickoffAt:match.kickoffAt,time:match.time,league:match.league,home:match.home,away:match.away,reason:detail?.reason||"本场返回结果未通过当前赛程与预测版本校验"};
   });
   setUnavailablePredictions(unavailable);setPredictionCoverage({officialMatches:predictionMatches.length,predictedMatches:reports.length,unavailableMatches:unavailable.length});
   setPredictionRows(reports);setPredictionVersion(data.version||null);setPredictionAiProvider("");setPredictionMeta({fetchedAt:data.fetchedAt||"",sourceUrl:data.sourceUrl||"",methodology:`预测版本 ${data.predictionId||"—"}；${data.methodology||""}`});
  }).catch(error=>{if(active){const fallback=buildOfficialPredictionFallback(predictionMatches,dataMeta.fetchedAt||new Date().toISOString());setPredictionRows(fallback.reports as PredictionReport[]);setPredictionVersion(fallback.version);setUnavailablePredictions(fallback.unavailableMatches);setPredictionCoverage(fallback.coverage);setPredictionAiProvider("体彩官方五玩法浏览器基线（外围赔率暂不可达）");setPredictionMeta({fetchedAt:fallback.version.generatedAt,sourceUrl:"https://www.sporttery.cn/",methodology:`预测版本 ${fallback.version.predictionId}；仅使用体彩官方五玩法去水概率，外围赔率恢复后将自动回到完整模型。`});setPredictionError(`外围赔率模型暂不可用：${error instanceof Error?error.message:"读取失败"}。当前已切换为体彩官方五玩法基线，未补造任何赔率。`)}}).finally(()=>{if(active)setPredictionLoading(false)});
  return()=>{active=false}
 },[view,dataLoading,dataState,liveMatches,predictionRetryNonce]);
 useEffect(()=>{
  if((view!=="predictions"&&view!=="market-predictions")||!predictionRows.length||!predictionMeta.fetchedAt||!predictionVersion)return;
  const capturedAt=new Date().toISOString(),historyRecordId=`browser-${predictionVersion.predictionId}-${predictionVersion.aiReviewedAt||predictionVersion.generatedAt}`;
 const saved:SavedPredictionSet={historyRecordId,predictionId:predictionVersion.predictionId,version:predictionVersion,date:shanghaiDate(),capturedAt,upstreamUpdatedAt:predictionMeta.fetchedAt,sourceFetchedAt:predictionMeta.fetchedAt,aiCompletedAt:predictionVersion.aiReviewedAt,decisionTiming:"unknown",aiProvider:predictionAiProvider,matches:predictionRows.map(row=>({predictionId:predictionVersion.predictionId,inputSnapshotId:predictionVersion.inputSnapshotId,baseModelVersion:predictionVersion.baseModelVersion,calibrationVersion:predictionVersion.calibrationVersion,predictionGeneratedAt:predictionVersion.generatedAt,fullScoreDistribution:row.fullScoreDistribution,shadowFullScoreDistribution:row.shadowFullScoreDistribution,shadowHadProbabilities:row.shadowHadProbabilities,expectedGoals:row.expectedGoals,appliedIntelligenceWeight:row.appliedIntelligenceWeight,intelligenceEvidence:row.intelligenceEvidence,id:row.id,officialMatchId:row.officialMatchId,salesDate:row.salesDate,kickoffAt:row.kickoffAt,homeTeamId:row.homeTeamId,awayTeamId:row.awayTeamId,homeTeamCode:row.homeTeamCode,awayTeamCode:row.awayTeamCode,officialMappingStatus:row.officialMappingStatus,marketEligibility:row.marketEligibility,league:row.league,time:row.time,matchDate:row.matchDate,home:row.home,away:row.away,matchStatus:row.matchStatus,isMock:row.isMock,oddsScores:row.oddsScores||row.fullScoreDistribution||row.scores,marketHadProbabilities:row.marketProbabilities?.length===3?["胜","平","负"].map((score,index)=>({score,probability:row.marketProbabilities![index]})):undefined,intelligenceScores:row.intelligenceScores,combinedScores:row.fullScoreDistribution||row.scores,intelligenceCoverage:row.intelligenceCoverage,aiSummary:row.aiSummary,aiRisk:row.aiRisk,hadProbabilities:row.hadProbabilities||[{score:"胜",probability:row.probabilities.home},{score:"平",probability:row.probabilities.draw},{score:"负",probability:row.probabilities.away}],hhadProbabilities:row.hhadProbabilities||(row.marketSignal?.modeledHhad?.length===3?["让胜","让平","让负"].map((score,index)=>({score,probability:row.marketSignal!.modeledHhad[index]})):undefined),totalGoalProbabilities:row.totalGoalProbabilities||(row.marketSignal?.modeledTotalGoals?.length===8?["0球","1球","2球","3球","4球","5球","6球","7+球"].map((score,index)=>({score,probability:row.marketSignal!.modeledTotalGoals![index]})):undefined),halfFullProbabilities:row.marketSignal?.modeledHalfFull?.length===9?["胜胜","胜平","胜负","平胜","平平","平负","负胜","负平","负负"].map((score,index)=>({score,probability:row.marketSignal!.modeledHalfFull![index]})):undefined,handicap:row.marketSignal?.officialHandicap||"",confidence:row.consensus.agreement==="较一致"?82:64,completeness:Math.max(1,10-row.missingCompanies.length-(row.intelligenceCoverage&&row.intelligenceCoverage>0?0:2)),singleModel:!row.intelligenceCoverage||row.intelligenceCoverage<=0,sourceFetchedAt:predictionMeta.fetchedAt,generatedAt:predictionVersion.generatedAt}))};
  void savePredictionSet(saved);
 },[view,predictionRows,predictionMeta.fetchedAt,predictionAiProvider,predictionVersion]);
 useEffect(()=>{if(!detailMatch)return;const game=matches.find(m=>m.id===detailMatch);if(!game?.matchId){setDetailData(null);return}let active=true;setDetailDataLoading(true);setDetailDataError("");fetch(`/api/sporttery/detail?matchId=${encodeURIComponent(game.matchId)}`,{cache:"no-store"}).then(async response=>{const data=await response.json();if(!response.ok)throw new Error(data.error||"单场详情读取失败");if(active)setDetailData(data)}).catch(error=>{if(active)setDetailDataError(error instanceof Error?error.message:"单场详情读取失败")}).finally(()=>{if(active)setDetailDataLoading(false)});return()=>{active=false}},[detailMatch,liveMatches]);
 const totalOdd=picks.reduce((n,p)=>n*p.odd,1),settled=records.filter(r=>r.result!=="待定"),invested=records.filter(r=>recordDate(r)===shanghaiDate()).reduce((n,r)=>n+r.stake,0),returned=records.reduce((n,r)=>n+(r.result==="命中"?r.stake*r.odd:0),0),hit=settled.length?Math.round(settled.filter(r=>r.result==="命中").length/settled.length*100):0;
 const dataStatusText:Record<DataState,string>={loading:"正在同步竞彩网…",success:"竞彩网数据已同步",stale:"正在显示过期的官方缓存",empty:"今日暂无官方赛事",error:"官方数据读取失败"};
 const failedPools=Object.entries(dataMeta.poolStatus||{}).filter(([,value])=>value.status==="failed").map(([pool])=>pool);
 function choose(matchId:string,market:string,label:string,odd:number){setPicks(x=>x.some(p=>p.matchId===matchId&&p.market===market&&p.label===label)?x.filter(p=>p.matchId!==matchId):[...x.filter(p=>p.matchId!==matchId),{matchId,market,label,odd}])}
 async function runAiAnalysis(game:Match){setAiLoading(true);setAiError("");setAiResult("");try{const response=await fetch("/api/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({match:{id:game.id,league:game.league,time:game.time,home:game.home,away:game.away,handicap:game.handicap||""},odds:Object.fromEntries((Object.keys(markets) as Market[]).map(m=>{const values=oddsFor(game,m);return[m,values?markets[m].map((label,i)=>({label,odd:values[i]||null})):null]})),knownFactors:{recentForm:game.form,riskNote:game.risk,standings:`主队 ${game.homeRank||"待核实"} / 客队 ${game.awayRank||"待核实"}`,injuries:"数据待同步",weather:"数据待同步",lineups:"数据待同步",playerReadiness:"数据待同步",headToHead:"数据待同步",coach:"数据待同步",venue:"数据待同步",referee:"数据待同步"},requestedAt:new Date().toISOString()})});const raw=await response.text();let data:any={};try{data=raw?JSON.parse(raw):{}}catch{/* 交由统一错误提示处理非 JSON 响应 */}if(!response.ok)throw new Error(data.error||`AI 服务暂不可用（HTTP ${response.status}）`);if(!data.text)throw new Error("AI 没有返回可显示的分析结果");setAiResult(data.text)}catch(error){setAiError(error instanceof Error?error.message:"AI 分析失败")}finally{setAiLoading(false)}}
 async function reviewTodayWithAi(){
  if(!predictionVersion){setPredictionAiError("当前预测版本尚未就绪，请刷新盘口预测后再复核。");return}
  setPredictionAiLoading(true);setPredictionAiError("");
  try{const requestedPredictionId=predictionVersion.predictionId,response=await fetch("/api/predictions/ai?provider=deepseek",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({version:predictionVersion,reports:predictionRows})}),data=await response.json();if(!response.ok)throw new Error(data.error||"DeepSeek 复核失败");if(data.version?.reviewForPredictionId!==requestedPredictionId)throw new Error("DeepSeek 返回结果不属于当前预测版本，已拒绝覆盖。");const reports=Array.isArray(data.reports)?data.reports.filter((row:PredictionReport)=>row.predictionId===data.version.predictionId):[];if(!reports.length)throw new Error("DeepSeek 未返回可保存的统一预测版本。");setPredictionRows(reports);setPredictionVersion(data.version);const coverages=reports.map((report:PredictionReport)=>Number(report.intelligenceCoverage)||0),averageCoverage=coverages.length?coverages.reduce((sum:number,value:number)=>sum+value,0)/coverages.length:0,appliedWeight=Math.max(...reports.map((report:any)=>Number(report.appliedIntelligenceWeight)||0),0);setPredictionAiProvider(`DeepSeek${data.model?` · ${data.model}`:""} · ${averageCoverage<=0?`无有效时效证据，数值概率未改变`:appliedWeight>0?`已通过未来验证并融合，平均证据覆盖 ${averageCoverage.toFixed(0)}%`:`证据覆盖 ${averageCoverage.toFixed(0)}%，但未来增益尚未验证，仅保留文字复核`}`)}catch(error){setPredictionAiError(error instanceof Error?error.message:"DeepSeek 复核失败")}finally{setPredictionAiLoading(false)}
 }
 function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setRecords(x=>[{id:Date.now(),date:shanghaiDate(),match:String(f.get("match")),type:String(f.get("type")||""),pick:String(f.get("pick")),stake:Number(f.get("stake")),odd:Number(f.get("odd")),result:"待定",note:String(f.get("note"))},...x]);e.currentTarget.reset()}
 return <main className={`view-${view}`}><div className="sky-shell"><header className="site-intro"><a className="brand" href="/matches"><span>球</span><span className="brand-copy"><b>竞彩研习室</b><small>理性分析 · 数据研究 · 提升认知</small></span></a><div className="top-utility"><a className="mobile-preview-link" href="/mobile-preview">手机预览</a><span className="weather" aria-label="天气装饰">☀ 24°C 晴</span><span className="avatar" aria-hidden="true">●</span></div></header><div className="topbar compact-nav"><nav><a className={view==="matches"?"active":""} href="/matches">▣ 今日比赛</a><a className={view==="predictions"?"active":""} href="/predictions">◉ AI预测</a><a className={view==="market-predictions"?"active":""} href="/market-predictions">◆ 盘口预测</a><a className={view==="recommendations"?"active":""} href="/recommendations">★ 今日推荐</a><ArchiveNavLink/></nav></div></div>
 <section className="hero" id="top"><div><p className="eyebrow">PERSONAL FOOTBALL LAB</p><h1>先研究，再决定。</h1><p className="lede">把赛程、赔率和自己的判断放在同一个地方。这里不提供“稳胆”，只帮助你看清风险。</p></div><div className="budget-card"><div><span>本月娱乐预算</span><strong>¥ {budget}</strong></div><div className="progress"><i style={{width:`${Math.min(100,invested/budget*100)}%`}}/></div><small>已记录 ¥{invested} · 剩余 ¥{Math.max(0,budget-invested)}</small></div></section>
 <section className="warning"><span>理性参与</span>彩票不是投资。请只使用能够完全承受损失的娱乐预算。比赛与赔率按需读取，不在本站保存。</section>
 <div className="match-repair-toolbar"><div><b>比赛缺失或玩法未补全？</b><span>补抓会重试官方五种玩法，并与当前列表合并，不会删除已经获取成功的比赛。</span></div><button type="button" onClick={repairMissingMatches} disabled={dataLoading}>{dataLoading?"正在补抓…":"补抓缺失场次"}</button>{repairNotice&&<em className={repairNotice.startsWith("补抓失败")?"failed":"success"}>{repairNotice}</em>}</div>
 <TodayRecommendations/>
 <MarketPredictionTable rows={predictionRows} coverage={predictionCoverage} unavailableMatches={unavailablePredictions} loading={predictionLoading} error={predictionError} aiError={predictionAiError} fetchedAt={predictionMeta.fetchedAt} sourceUrl={predictionMeta.sourceUrl} aiProvider={predictionAiProvider} aiLoading={predictionAiLoading} onAiReview={reviewTodayWithAi} onRetryUnavailable={retryUnavailablePredictionData} retryingUnavailable={dataLoading||predictionLoading} retryMessage={repairNotice}/>
 <AiPredictionReport rows={predictionRows} coverage={predictionCoverage} unavailableMatches={unavailablePredictions} loading={predictionLoading} error={predictionError} aiError={predictionAiError} fetchedAt={predictionMeta.fetchedAt} sourceUrl={predictionMeta.sourceUrl} methodology={predictionMeta.methodology} aiProvider={predictionAiProvider} aiLoading={predictionAiLoading} onAiReview={()=>reviewTodayWithAi()} onRetryUnavailable={retryUnavailablePredictionData} retryingUnavailable={dataLoading||predictionLoading} retryMessage={repairNotice}/>
 <section className="predictions-page"><div className="section-head prediction-report-head"><div><p className="eyebrow">DAILY SCORE OUTLOOK</p><h2>今日比分预测报告</h2><p>体彩赔率 + 36*、ＳＢ/*、平* 欧亚大小球联动</p></div><div className="prediction-actions"><button onClick={reviewTodayWithAi} disabled={predictionLoading||predictionAiLoading||!predictionRows.length}>{predictionAiLoading?"AI 正在复核…":"AI 复核全部比赛"}</button>{predictionMeta.sourceUrl&&<a href={predictionMeta.sourceUrl} target="_blank" rel="noreferrer">赔率来源 ↗</a>}</div></div>{predictionError&&<div className="data-fallback">{predictionError}</div>}{predictionAiError&&<div className="data-fallback">AI 复核未完成：{predictionAiError}；当前仍展示可用的赔率模型基线。</div>}<div className="prediction-disclaimer"><b>{predictionAiProvider?`已由 ${predictionAiProvider} 复核`:`赔率模型基线（尚未调用 AI 复核）`}</b><span>{predictionMeta.methodology}</span>{predictionMeta.fetchedAt&&<small>数据读取 {new Date(predictionMeta.fetchedAt).toLocaleString("zh-CN")}</small>}</div>{predictionLoading?<div className="results-empty">正在同步三家公司赔率并计算全部场次…</div>:<div className="daily-prediction-list">{predictionRows.map((row,index)=><article className="daily-prediction-card" key={row.id}><header><div><span className="prediction-rank">#{index+1}</span><b>{row.id}</b><span>{row.league}</span><time>{row.time.slice(5,16)}</time></div><h3>{row.home}<i>VS</i>{row.away}</h3><span className={row.consensus.agreement==="较一致"?"agreement good":"agreement"}>{row.consensus.agreement}</span></header><div className="score-picks">{row.scores.map((score,scoreIndex)=><div key={score.score}><small>{scoreIndex===0?"首选":"候选"}</small><strong>{score.score}</strong><b>{score.probability.toFixed(1)}%</b></div>)}</div><div className="prediction-evidence"><div><small>胜平负共识</small><b>主 {row.probabilities.home.toFixed(1)}% · 平 {row.probabilities.draw.toFixed(1)}% · 客 {row.probabilities.away.toFixed(1)}%</b></div><div><small>盘口中位数</small><b>主队 {row.consensus.handicap>0?"+":""}{row.consensus.handicap} · 总球 {row.consensus.totalLine}</b></div><div><small>模型预期进球</small><b>{row.expectedGoals.home.toFixed(2)} : {row.expectedGoals.away.toFixed(2)}</b></div></div><PredictionMarketSignals row={row}/><div className="company-odds-table legacy-company-table"><div className="company-odds-row heading"><span>公司</span><span>主胜</span><span>平</span><span>客胜</span><span>让球</span><span>大小</span></div>{row.companies.map(company=><div className="company-odds-row" key={company.companyId}><b>{company.company}</b><span>{company.win.toFixed(2)}</span><span>{company.draw.toFixed(2)}</span><span>{company.lose.toFixed(2)}</span><span>{company.handicap>0?"+":""}{company.handicap}</span><span>{company.total}</span></div>)}</div>{row.aiSummary&&<p className="ai-review"><b>AI 联动判断：</b>{row.aiSummary}</p>}{row.aiRisk&&<p className="ai-risk"><b>最大风险：</b>{row.aiRisk}</p>}{row.missingCompanies.length>0&&<p className="missing-source">缺少公司编号 {row.missingCompanies.join("、")} 的当前赔率，本场置信度已降低。</p>}</article>)}</div>}<p className="prediction-footnote">概率为“精确比分”的模型估计，候选概率不会加总为 100%。赔率变化反映市场预期，不代表确定赛果；临场伤停、首发、天气与裁判若未同步，应另行复核。</p></section>
 <section className="results-page"><div className="section-head"><div><p className="eyebrow">MATCH RESULTS</p><h2>竞彩足球赛果</h2></div><div className="results-query"><label>查询日期<input type="date" value={resultDate} min={shanghaiDate(-29)} max={shanghaiDate()} onChange={event=>setResultDate(event.target.value)}/></label><span>支持最近 30 天</span></div></div>{resultError&&<div className="data-fallback">{resultError}</div>}{resultLoading?<div className="results-empty">正在读取竞彩网赛果…</div>:resultRows.length?<div className="results-table-wrap"><table className="results-table"><thead><tr><th>场次</th><th>联赛</th><th>对阵</th><th>半场</th><th>全场赛果</th><th>让球</th></tr></thead><tbody>{resultRows.map(row=><tr key={row.matchId||`${row.id}-${row.home}`}><td><strong>{row.id}</strong></td><td><span className="league-chip">{row.league||"—"}</span></td><td><div className="result-versus"><strong>{row.home}</strong><span>vs</span><strong>{row.away}</strong></div></td><td>{row.halfScore}</td><td><b className="full-score">{row.fullScore}</b></td><td>{row.handicap||"—"}</td></tr>)}</tbody></table></div>:!resultError&&<div className="results-empty">该日期暂无已公布赛果</div>}<p className="results-source">数据来源：中国体育彩票·竞彩网；页面按需查询，不保存赛果数据。</p></section>
 <div className="match-filters"><label><span>竞彩场次 / 开售日</span><select value={dateFilter} onChange={event=>{const nextDate=event.target.value;setDateFilter(nextDate);const nextMatch=allMatches.find(match=>nextDate==="全部场次"||matchDateKey(match)===nextDate);if(nextMatch)setBetMatch(nextMatch.id)}}><option value="全部场次">全部场次</option>{matchDates.map(date=><option key={date} value={date}>{matchDateLabel(date)}</option>)}</select></label><label><span>联赛筛选</span><select value={leagueFilter} onChange={event=>setLeagueFilter(event.target.value)}>{leagues.map(league=><option key={league}>{league}</option>)}</select></label><label className="match-search"><span>搜索比赛</span><input value={jumpMatch} onChange={event=>setJumpMatch(event.target.value)} placeholder="比赛序号或球队名称"/></label><label className="hide-closed"><input type="checkbox" checked={hideClosed} onChange={event=>setHideClosed(event.target.checked)}/><span>隐藏已截止比赛</span></label><small>当前显示 {matches.length} 场</small></div>
 <section className="section" id="matches"><div className="section-head"><div><p className="eyebrow">TODAY&apos;S BOARD</p><h2>今日比赛</h2></div><div className="data-status"><span className={dataState==="success"?"live":dataState==="loading"?"loading":"error"}>{dataStatusText[dataState]}</span>{(dataMeta.upstreamUpdatedAt||dataMeta.fetchedAt)&&<small>更新时间 {dataMeta.upstreamUpdatedAt||new Date(dataMeta.fetchedAt).toLocaleString("zh-CN")}</small>}<button onClick={()=>refreshSporttery()} disabled={dataLoading}>刷新</button></div></div>{dataState==="stale"&&<div className="data-fallback">官方接口暂不可用，当前仅展示上次成功同步的过期缓存；全部赔率选择和新预测已暂停。</div>}{dataState==="error"&&<div className="data-fallback">{dataError}。未加载演示比赛或示例赔率，请稍后刷新。</div>}{failedPools.length>0&&<div className="data-fallback">部分玩法读取失败：{failedPools.join("、")}。其他已成功玩法继续展示，失败玩法不会使用示例赔率。</div>}<div className="match-table"><div className="match-table-head"><span>比赛序号</span><span>联赛</span><span>开赛时间</span><span>主队 vs 客队</span><b className="group-title had">胜平负</b><b className="group-title hhad">让球胜平负</b><span>更多</span></div>{matches.map(m=>{const had=oddsFor(m,"胜平负"),hhad=oddsFor(m,"让球胜平负"),status=(m.matchStatus||"").toLowerCase(),closed=status&&status!=="selling",locked=dataState!=="success";const statusText=/cancel/.test(status)?"取消":/delay|postpone/.test(status)?"延期":closed?"已截止":"销售中";return <article className={`match-row ${closed?"closed":""}`} key={m.id}><div className="match-id"><strong>{m.id}</strong><span className={`match-status ${closed?"stopped":"selling"}`}>{statusText}</span></div><span className={`league-chip league-tone-${Math.max(0,leagues.indexOf(m.league)-1)%20}`}>{m.league}</span><time>{m.time}</time><div className="table-teams"><strong>{m.home}</strong><i>VS</i><strong>{m.away}</strong></div><div className="odds-group had-group"><small>{had?"胜平负":m.marketStatus?.["胜平负"]==="failed"?"读取失败":"未开售"}</small>{["胜","平","负"].map((label,i)=>{const odd=had?.[i]||0;return <button disabled={locked||closed||!odd} className={picks.some(p=>p.matchId===m.id&&p.market==="胜平负"&&p.label===label)?"picked":""} key={label} onClick={()=>choose(m.id,"胜平负",label,odd)}><span>{label}</span><b>{odd?odd.toFixed(2):"—"}</b></button>})}</div><span className={`handicap-tag ${Number(m.handicap)>0?"positive":Number(m.handicap)<0?"negative":"neutral"}`}>主队 {handicapText(m.handicap)}</span><div className="odds-group hhad-group"><small>{hhad?"让球胜平负":m.marketStatus?.["让球胜平负"]==="failed"?"读取失败":"未开售"}</small>{["让胜","让平","让负"].map((label,i)=>{const odd=hhad?.[i]||0;return <button disabled={locked||closed||!odd} className={picks.some(p=>p.matchId===m.id&&p.market==="让球胜平负"&&p.label===label)?"picked":""} key={label} onClick={()=>choose(m.id,"让球胜平负",label,odd)}><span>{label}</span><b>{odd?odd.toFixed(2):"—"}</b></button>})}</div><button className="detail-entry" onClick={()=>{setDetailMatch(m.id);setDetailMarket("胜平负")}}>查看</button>{m.remark&&<p className="match-remark">{m.remark}</p>}</article>})}{!matches.length&&!dataLoading&&<div className="results-empty compact-empty">{dataState==="empty"?"今日暂无官方赛事":dataState==="error"?"官方数据读取失败，当前没有可展示的比赛":"没有符合筛选条件的比赛"}</div>}</div></section>
 <section className="market-center" id="all-markets"><div className="section-head"><div><p className="eyebrow">ALL FOOTBALL MARKETS</p><h2>竞彩足球全部玩法</h2></div><span className="muted">均以 90 分钟及伤停补时赛果为准</span></div><div className="match-switch">{matches.map(m=><button className={betMatch===m.id?"active":""} key={m.id} onClick={()=>setBetMatch(m.id)}><b>{m.id}</b><span>{m.home} vs {m.away}</span></button>)}</div><div className="market-tabs">{(Object.keys(markets) as Market[]).map(m=>{const game=matches.find(item=>item.id===betMatch)||matches[0],state=game?.marketStatus?.[m],available=Boolean(oddsFor(game,m));return <button className={betMarket===m?"active":""} data-availability={available?"available":state||"unavailable"} key={m} onClick={()=>setBetMarket(m)}>{m}{!available&&<small>{state==="failed"?"失败":"未开售"}</small>}</button>})}</div>{(()=>{const game=matches.find(m=>m.id===betMatch)||matches[0];if(!game)return <div className="results-empty compact-empty">{dataState==="loading"?"正在读取官方比赛…":dataState==="empty"?"今日暂无官方赛事":"当前没有可用的官方比赛"}</div>;const activeOdds=oddsFor(game,betMarket),marketState=game.marketStatus?.[betMarket],locked=dataState!=="success";return <>{betMarket==="让球胜平负"&&<div className={`handicap-note ${Number(game.handicap)>0?"positive":Number(game.handicap)<0?"negative":"neutral"}`}><b>{betMatch} 主队（{game.handicap==null||game.handicap===""?"官方暂未发布":handicapText(game.handicap)}）</b><span>让球数随官方数据同步</span></div>}{!activeOdds?<div className="results-empty compact-empty">{marketState==="failed"?`${betMarket}接口读取失败，其他玩法不受影响。`:`${betMarket}暂未开售或暂无官方赔率。`}</div>:<div className={`market-options ${betMarket==="比分"?"scores":""}`}>{markets[betMarket].map((label,i)=>{const odd=activeOdds[i]||0,on=picks.some(p=>p.matchId===betMatch&&p.market===betMarket&&p.label===label);return <button disabled={locked||!odd} className={on?"selected":""} key={label} onClick={()=>choose(betMatch,betMarket,label,odd)}><span>{label}</span><strong>{odd?odd.toFixed(2):"暂无数据"}</strong></button>})}</div>}</>})()}<p className="market-rule">同一场比赛在组合中仅保留一个选择；点击其他玩法会替换该场原选择。赔率来自中国体育彩票竞彩网，提交前请再次核对官方页面。</p></section>
 <section className="section calculator" id="builder"><div className="section-head"><div><p className="eyebrow">COMBINATION LAB</p><h2>混合过关模拟</h2></div><button className="text-btn" onClick={()=>setPicks([])}>清空选择</button></div><div className="calc-grid"><div className="pick-list">{picks.length?picks.map(p=><div key={p.matchId}><span><b>{p.matchId}</b><small>{p.market} · {p.label}</small></span><strong>{p.odd.toFixed(2)}</strong><button onClick={()=>setPicks(x=>x.filter(v=>v.matchId!==p.matchId))}>×</button></div>):<div className="empty">从“全部玩法”中选择结果，组合会出现在这里。</div>}</div><aside className="calc-panel"><label>计划投入（元）<input type="number" min="2" step="2" value={stake} onChange={e=>setStake(Number(e.target.value))}/></label><div><span>组合方式</span><strong>{picks.length<=1?"单关":`${picks.length} 串 1`}</strong></div><div><span>组合赔率</span><strong>{picks.length?totalOdd.toFixed(2):"—"}</strong></div><div className="return"><span>理论返还</span><strong>¥ {picks.length?(stake*totalOdd).toFixed(2):"0.00"}</strong></div><small>仅供数学模拟；实际可售玩法与过关方式以官方公布为准。</small></aside></div></section>
 <section className="fund-tool" id="fund-tool"><div className="section-head"><div><p className="eyebrow">CAPITAL & ROUND PLANNER</p><h2>投入收益与轮次管理</h2></div><a href="/tools/investment-calculator.html" target="_blank" rel="noreferrer">在独立页面打开 ↗</a></div><div className="tool-intro"><div><b>已联动原有投入收益计算器</b><p>用于管理轮次、投入模版、中奖标记、投入产出报表和 CSV 导出。该工具使用独立的浏览器本地数据，不会自动改变上方竞彩组合。</p></div><span>个人本地工具</span></div><details><summary><span>展开投入收益计算器</span><small>首次打开会使用一套独立数据</small></summary><iframe src="/tools/investment-calculator.html" title="投入收益计算器" loading="lazy"/></details></section>
 <section className="journal" id="journal"><div className="section-head"><div><p className="eyebrow">MY JOURNAL</p><h2>投注与复盘记录</h2></div></div><div className="stats"><div><span>累计投入</span><strong>¥{invested}</strong></div><div><span>已结算返还</span><strong>¥{returned.toFixed(2)}</strong></div><div><span>命中率</span><strong>{hit}%</strong></div><div><span>账面差额</span><strong className={returned-invested>=0?"positive":"negative"}>{returned-invested>=0?"+":""}¥{(returned-invested).toFixed(2)}</strong></div></div><form className="record-form record-form-expanded" onSubmit={add}><input name="match" required placeholder="比赛，如：主队 vs 客队"/><select name="type" defaultValue=""><option value="">类型（可选）</option><option>比分</option><option>进球数</option><option>2串1</option><option>3串1</option><option>4串1</option><option>5串1</option><option>其他串</option></select><select name="pick"><option>胜</option><option>平</option><option>负</option></select><input name="odd" required type="number" step="0.01" min="1" placeholder="赔率"/><input name="stake" required type="number" step="2" min="2" placeholder="投入"/><input name="note" placeholder="备注（可选）"/><button>添加记录</button></form><div className="table-wrap"><table className="records-table"><thead><tr><th>比赛</th><th>类型</th><th>选择</th><th>投入 / 赔率</th><th>结果</th><th>备注</th><th/></tr></thead><tbody>{records.map(r=><tr key={r.id}><td><strong>{r.match}</strong></td><td><span className="record-type">{r.type||"未分类"}</span></td><td>{r.pick}</td><td>¥{r.stake} / {r.odd.toFixed(2)}</td><td><select className={`status ${r.result}`} value={r.result} onChange={e=>setRecords(x=>x.map(v=>v.id===r.id?{...v,result:e.target.value as Rec["result"]}:v))}><option>待定</option><option>命中</option><option>未中</option></select></td><td className="record-note">{r.note||"—"}</td><td><button className="delete" onClick={()=>setRecords(x=>x.filter(v=>v.id!==r.id))}>删除</button></td></tr>)}</tbody></table></div></section>
 {detailMatch&&(()=>{
   const game=matches.find(m=>m.id===detailMatch)!;
   const selectedMarketOdds=oddsFor(game,detailMarket),odds=(selectedMarketOdds||[]).filter(value=>value>0);
   const raw=odds.map(o=>1/o),sum=raw.reduce((a,b)=>a+b,0);
   const hadOdds=oddsFor(game,"胜平负")||[],hadRaw=hadOdds.map(odd=>odd>0?1/odd:0),hadSum=hadRaw.reduce((a,b)=>a+b,0),hadProbabilities=hadRaw.map(value=>hadSum?value/hadSum*100:0);
   const rankedHad=["主胜","平局","客胜"].map((label,index)=>({label,probability:hadProbabilities[index]||0})).sort((a,b)=>b.probability-a.probability),leadGap=rankedHad[0].probability-rankedHad[1].probability;
   const history=detailData?.oddsHistory?.[0],hadHistory=history?.hadList||[],hhadHistory=history?.hhadList||[];
   return <div className="detail-overlay" role="dialog" aria-modal="true" aria-label="比赛详情"><div className="detail-sheet">
     <header><div><p className="eyebrow">MATCH INTELLIGENCE</p><h2>{game.home} <span>vs</span> {game.away}</h2><small>{game.id} · {game.league} · {game.time}</small></div><button aria-label="关闭" onClick={()=>setDetailMatch(null)}>×</button></header>
     <div className="sync-status"><b>基础概率：竞彩网赔率归一化</b><span>官方赔率仅按需读取、不落库；点击下方按钮可调用 AI 联网分析。</span></div>
     <section className="official-odds-detail"><div className="official-odds-head"><div><h3>竞彩网单场固定奖金</h3><span>{detailDataLoading?"正在读取该场详细赔率…":detailDataError||`已读取 ${hadHistory.length+hhadHistory.length} 条胜平负赔率变动`}</span></div>{game.matchId&&<a href={detailData?.sourceUrl||`https://www.sporttery.cn/jc/zqdz/index.html?showType=3&mid=${game.matchId}`} target="_blank" rel="noreferrer">查看官方原页 ↗</a>}</div>{detailDataError&&<div className="detail-data-error">{detailDataError}，当前概率仍使用比赛列表的最新赔率。</div>}{!detailDataLoading&&!detailDataError&&(hadHistory.length||hhadHistory.length)?<div className="odds-history-grid">{[["胜平负",hadHistory],["让球胜平负",hhadHistory]] .map(([title,points])=><div className="odds-history" key={String(title)}><b>{String(title)}</b><div className="odds-history-labels"><span>更新时间</span><span>胜</span><span>平</span><span>负</span></div>{(points as OddsPoint[]).slice(-6).reverse().map((point,index)=><div className="odds-history-row" key={`${point.updateDate}-${point.updateTime}-${index}`}><time>{point.updateDate?.slice(5)} {point.updateTime?.slice(0,5)}</time><span className={point.hf==="1"?"up":point.hf==="-1"?"down":""}>{point.h||"—"}</span><span className={point.df==="1"?"up":point.df==="-1"?"down":""}>{point.d||"—"}</span><span className={point.af==="1"?"up":point.af==="-1"?"down":""}>{point.a||"—"}</span></div>)}</div>)}</div>:null}</section>
     <section className="analysis-flow"><div><b>① 胜平负</b><span>确定基本方向</span></div><i>→</i><div><b>② 让球</b><span>判断优势大小</span></div><i>→</i><div><b>③ 总进球</b><span>判断比赛节奏</span></div><i>→</i><div><b>④ 比分</b><span>验证比分范围</span></div><i>→</i><div><b>⑤ 半全场</b><span>验证比赛过程</span></div></section>
     <div className="ai-action"><div><b>AI 综合分析</b><span>赔率、伤停、天气、首发、球员状态、排名、交锋、教练、场地和裁判逐项审计；无法核实的因素必须披露。</span></div><button disabled={aiLoading||dataState!=="success"} onClick={()=>runAiAnalysis(game)}>{aiLoading?"正在分析…":dataState!=="success"?"等待最新官方数据":"调用 AI 重新分析"}</button></div>
     {aiError&&<div className="ai-error">{aiError}</div>}
     {aiResult&&<div className="ai-result"><div><b>AI 综合预测结果</b><button onClick={()=>setAiResult("")}>清除</button></div><pre>{aiResult}</pre></div>}
     <section className="detail-summary"><div><span>市场基础方向</span><strong>{rankedHad[0].label}</strong><small>去水隐含概率 {rankedHad[0].probability.toFixed(1)}%</small></div><div><span>方向领先幅度</span><strong>{leadGap.toFixed(1)}%</strong><small>领先第二选项的概率差</small></div><div><span>基础不确定性</span><strong>{leadGap>=20?"较低":leadGap>=10?"中等":"较高"}</strong><small>仍需结合让球、进球数与临场信息</small></div></section>
     <h3>多因素输入</h3><div className="factor-grid">{[["体彩赔率","已纳入","28%"],["伤病停赛","AI检索","12%"],["天气状况","AI检索","5%"],["预计首发","AI检索","12%"],["球员准备度","AI检索","8%"],["排名与积分","AI检索","10%"],["历史交锋","AI检索","6%"],["主教练影响","AI检索","6%"],["场地与主客场","AI检索","8%"],["裁判尺度","AI检索","5%"]].map(f=><div key={f[0]}><span>{f[0]}</span><b>{f[1]}</b><small>建议权重 {f[2]}</small></div>)}</div>
     <div className="prediction-head"><div><h3>市场隐含概率基线</h3><small>已按当前玩法去除返还率影响；调用 AI 后，请与上方模型概率和差值对照</small></div><div className="market-tabs">{(Object.keys(markets) as Market[]).map(m=><button className={detailMarket===m?"active":""} key={m} onClick={()=>setDetailMarket(m)}>{m}</button>)}</div></div>
     {!selectedMarketOdds?<div className="results-empty compact-empty">{game.marketStatus?.[detailMarket]==="failed"?`${detailMarket}接口读取失败。`:`${detailMarket}暂未开售或暂无官方赔率。`}</div>:<div className={`prediction-grid ${detailMarket==="比分"?"many":""}`}>{markets[detailMarket].map((label,i)=>{const odd=selectedMarketOdds[i]||0;if(!odd)return <button disabled key={label}><span>{label}</span><strong>暂无数据</strong></button>;const probability=sum?(1/odd)/sum*100:0;return <button disabled={dataState!=="success"} key={label} onClick={()=>choose(game.id,detailMarket,label,odd)}><span>{label}</span><strong>{probability.toFixed(1)}%</strong><small>体彩赔率 {odd.toFixed(2)}</small><i><em style={{width:`${Math.min(100,probability*2)}%`}}/></i></button>})}</div>}
     <div className="method-note"><b>阅读顺序与限制</b><p>先用胜平负判断方向，再用让球判断能否拉开差距，以总进球判断节奏，最后用比分和半全场交叉验证。赔率下降只代表市场预期变化，不等于结果确定；AI 分析须披露缺失信息，任何低概率事件仍可能发生。</p></div>
   </div></div>
 })()}
 <section className="field-footer"><div><p className="eyebrow">PLAY WITH PERSPECTIVE</p><h2>把判断留给数据，<br/>把热爱留在球场。</h2><p>每一次选择都记录依据、概率与风险。保持预算，长期复盘。</p><a href="#top">返回今日比赛 ↑</a></div></section>
 <footer className="site-footer"><span>竞彩研习室 · 个人本地版</span><span>预测快照与投注记录仅保存在当前浏览器中</span></footer></main>}








